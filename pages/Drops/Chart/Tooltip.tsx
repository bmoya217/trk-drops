import { ChartsAxisContentProps } from "@mui/x-charts";
import { FC } from "react";

const Tooltip: FC<ChartsAxisContentProps> = ({ ...props }) => {
  //   useEffect(() => {
  //     // @ts-ignore
  //     props.onChange(props.dataIndex);
  //   }, [props.dataIndex]);

  return null;
};

export default Tooltip;
